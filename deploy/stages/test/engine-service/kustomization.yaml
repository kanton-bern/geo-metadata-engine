apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: as6-geo-metadaten-${stage}
resources:
  - ../../base/engine-service/
  - secrets/
configMapGenerator:
- name: engine-service-stage-values
  files:
  - values.yaml
  options:
    disableNameSuffixHash: true
