---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
- name: engine-service-stage-secrets
  files:
  - values.yaml=secret-values.yaml
  type: "Opaque"
  options:
    disableNameSuffixHash: true
