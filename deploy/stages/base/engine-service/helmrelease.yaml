apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: engine-service
  labels:
    prometheus.alerting.bedag.ch: "false"
spec:
  interval: 5m
  chart:
    spec:
      chart: common
      version: "12.7.0"
      sourceRef:
        kind: HelmRepository
        name: bedag-public
        namespace: flux-public
  releaseName: engine-service-${stage}
  install:
    remediation:
      # dont retry install
      retries: 0
      # do not cleanup if helm install fails
      remediateLastFailure: false
  upgrade:
    remediation:
      # dont retry installs
      retries: 0
      # do not cleanup if helm install fails
      remediateLastFailure: false
  valuesFrom:
    - kind: ConfigMap
      name: engine-service-values
      optional: true
    - kind: ConfigMap
      name: engine-service-stage-values
      optional: true
    - kind: Secret
      name: engine-service-stage-secrets
      optional: true
